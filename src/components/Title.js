import React from "react"

const Title = ({ title }) => {
  return (
    <h2 className="text-3xl md:text-6xl leading-14 text-center font-PlayFair font-medium">
      {title}
    </h2>
  );
};

export default Title;
